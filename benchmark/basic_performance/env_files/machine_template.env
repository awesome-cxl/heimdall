disable_prefetch=True
boost_cpu=True
core_num_per_socker=16
dimm_physical_start_addr=for cache analysis test, not for the bw latency test
dimm_test_size=for cache analysis test, not for the bw latency test
# if you don't know the core_num_per_socker, you can find it with this cmd
#x86: lscpu | grep "Core(s) per socket"
#arm: lscpu | grep "Core(s) per cluster"
